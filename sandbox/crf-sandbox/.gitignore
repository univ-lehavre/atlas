# Generated by `pnpm test:setup` — contains REDCap API tokens scoped to
# the local docker instance, but we keep it out of git on principle
# (the template is in .env.test.example).
docker/config/.env.test

# REDCap upstream archives downloaded by docker:prepare
upstream/
