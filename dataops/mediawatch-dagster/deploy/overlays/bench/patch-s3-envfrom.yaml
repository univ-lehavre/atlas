# Banc : un Secret unique (SeaweedFS) porte AWS_* ET BUCKET_*.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mediawatch-dagster
  namespace: dagster
spec:
  template:
    spec:
      containers:
        - name: grpc
          envFrom:
            - secretRef:
                name: mediawatch-s3-access
