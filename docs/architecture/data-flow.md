# Flux de données

_(à rédiger pour un public non-expert dans une PR ultérieure)_

Cette page décrira comment les données circulent entre REDCap (source d'enregistrement clinique/recherche), les applications SvelteKit (UI), et Appwrite (authentification + métadonnées applicatives), avec un schéma simple par cas d'usage.
