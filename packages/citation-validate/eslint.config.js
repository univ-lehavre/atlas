import { scripts } from "@univ-lehavre/atlas-shared-config/eslint";

export default scripts({
  // TODO(chasset, 2026-06-01): migrer src/prompt/ vers cli/biblio — @clack/prompts a sa place dans cli/ (cf. ADR 0019)
  // architectureCategory: "packages",
});
