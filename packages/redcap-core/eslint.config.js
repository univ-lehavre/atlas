import { scripts } from '@univ-lehavre/atlas-shared-config/eslint';

// Using scripts preset for pragmatic functional code.
// The code is designed as pure functions but uses some imperative patterns
// (loops, conditionals) which are more readable than fully functional alternatives.
export default scripts();
